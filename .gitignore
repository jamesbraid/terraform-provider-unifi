*.swp

.vscode/settings.json
dist
/terraform-provider-*
.terraform

.terraform.lock.hcl
/*.tf


*.log
.idea
/.deprecated*

# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov
junit.xml

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Editor/IDE
# .idea/
# .vscode/

# Build artifacts
bin/
dist/
release/

# Logs
*.log

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

/ironic-metadata

__debug_bin*
.vscode/settings.json

.github/instructions/*.md
.github/.bak

.test

# Executables left at the repository root by `go build ./cmd/<name>`.
#
# Without an -o, go build writes the binary into the working directory named
# after the command's directory. Run from the root -- which is where every
# other go command here is run -- that leaves a multi-megabyte executable
# beside the source, untracked and easy to sweep into a `git add`. One such
# commit added five of them, 19 MB, and they reached a shared branch.
#
# THE FIRST ATTEMPT AT THIS RULE SAT DEAD IN THIS FILE FOR ITS ENTIRE LIFE:
# a missing newline joined this rule's ignore pattern to an unrelated entry
# above it, producing one path that matched nothing rather than two that
# matched something. It was split and repaired in 9c654f56. This list is the
# complete set of cmd/ names, kept in sync so a partial one never reads as
# an omission.
/action-gen
/generated-value-strip
/list-resource-gen
/metadata-contract-gen
/nested-custom-type-strip
/nested-type-dedup
/provider-spec-compiler
/sdk-bootstrap

# provider-codegen's compiler intermediates. *.provider-code-spec.json is the
# hand-off between provider-spec-compiler and tfplugingen-framework/
# list-resource-gen/action-gen -- go generate writes and reads it within the
# same run, so it never needs to survive between them. *.impact.json is
# retired: nothing in this tree has emitted or read one for a while: the
# compiler's Result no longer has the field. Neither belongs in the diff a
# reviewer reads; *.mapping.json is not here because descriptor_mapping_test.go
# reads the committed copy directly, as its own independent record.
provider-codegen/generated/*.impact.json
*.provider-code-spec.json

# provider-codegen/bootstrap's structural projections of the pinned go-unifi
# SDK. sdk-bootstrap derives these fresh from the pinned commit at the head
# of every go generate run, so committing them duplicated what regeneration
# already reproduces deterministically -- the real drift gate is each
# policy file's own source_specification_sha256 pin, checked against a
# freshly-derived digest every run; a stale checked-in bootstrap sat next to
# that gate without strengthening it. Three files stay committed anyway:
# go-unifi-v1.102.0-dns-record.json has no go:generate line that reproduces
# it -- it is a frozen snapshot from a go-unifi commit nothing else in this
# tree still imports; go-unifi-v1.103.0-vpn-server.json and
# go-unifi-v1.103.0-firewall-zone.json are read directly by
# secret_candidate_test.go and resourcekit_contract_test.go, the same reason
# *.mapping.json above stays committed rather than gitignored.
provider-codegen/bootstrap/*.json
!provider-codegen/bootstrap/go-unifi-v1.102.0-dns-record.json
!provider-codegen/bootstrap/go-unifi-v1.103.0-vpn-server.json
!provider-codegen/bootstrap/go-unifi-v1.103.0-firewall-zone.json
