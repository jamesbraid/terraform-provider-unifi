name: CI/CD

on:
  pull_request:
    paths:
    - "**.go"
    - "go.mod"
    - "go.sum"
    - ".github/workflows/ci.yaml"
  push:
    branches:
    - "main"
    paths:
    - "**.go"
    - "go.mod"
    - "go.sum"
    - ".github/workflows/ci.yaml"

# contents: read is needed to check out the repository.
# id-token: write is needed for Codecov OIDC uploads.
permissions:
  contents: read
  id-token: write

jobs:
  lint:
    name: GolangCI Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-go@3041bf56c941b39c61721a86cd11f3bb1338122a # v5.2.0
        with:
          go-version-file: "go.mod"
          cache: true
      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a # v9.3.0
        with:
          version: latest
          # `latest` can introduce new checks without a code change; only-new-issues
          # keeps pre-existing debt from blocking PRs that didn't introduce it.
          only-new-issues: true

  build:
    name: Build
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
    - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
    - uses: actions/setup-go@3041bf56c941b39c61721a86cd11f3bb1338122a # v5.2.0
      with:
        go-version-file: "go.mod"
        cache: true
    - run: go mod download
    - run: go build -v .

  generate:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
    - uses: actions/setup-go@3041bf56c941b39c61721a86cd11f3bb1338122a # v5.2.0
      with:
        go-version-file: "go.mod"
        cache: true

    # Installs Terraform to avoid tfplugindocs downloading one at runtime (which
    # hits checkpoint-api.hashicorp.com and can time out). terraform_wrapper: false
    # because the wrapper reformats CLI output that the schema export can't read.
    - uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
      with:
        terraform_wrapper: false

    - run: go generate ./...
    - name: git diff
      run: |
        git diff --compact-summary --exit-code || \
          (echo; echo "Unexpected difference in directories after code generation. Run 'go generate ./...' command and commit."; exit 1)
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
    - uses: actions/setup-go@3041bf56c941b39c61721a86cd11f3bb1338122a # v5.2.0
      with:
        go-version-file: "go.mod"
        cache: true
    - run: go mod download

    - name: Unit tests
      if: always()
      # -race requires cgo; the coverage profile feeds Codecov below.
      run: go tool gotestsum --junitfile junit.xml -- -coverprofile=coverage.txt -covermode=atomic -race -count=1 -timeout 120m -v ./...
      env:
        CGO_ENABLED: "1"

    - name: Generate HTML coverage report
      if: always()
      run: go tool cover -html=coverage.txt -o coverage.html

    - name: Upload coverage report
      if: always()
      uses: actions/upload-artifact@v7
      with:
        name: coverage-report
        path: |
          coverage.txt
          coverage.html
          junit.xml

    - name: Upload test results to Codecov
      if: ${{ !cancelled() }}
      uses: codecov/test-results-action@v1
      with:
        use_oidc: true
        flags: unit

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v7
      with:
        use_oidc: true
        flags: unit
