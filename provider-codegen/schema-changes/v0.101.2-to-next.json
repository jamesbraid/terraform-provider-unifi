{
  "format_version": 1,
  "from_version": "0.101.2",
  "to_version": "next",
  "entries": [
    {
      "surface": "unifi_network",
      "attribute": "lte_lan",
      "field": "description",
      "old": "Whether this network/VLAN stays active when the gateway fails over to a UniFi LTE (cellular) backup WAN. Maps to the controller's `lte_lan_enabled` flag and only matters when a UniFi LTE failover device is in use; otherwise it is cosmetic. Defaults to `true` (network stays available during LTE failover); set to `false` to disable it while on the LTE backup link. The controller may set this automatically, which is why existing networks can show differing values.",
      "new": "Whether this network/VLAN stays active when the gateway fails over to a UniFi LTE (cellular) backup WAN. Maps to the controller's `lte_lan_enabled` flag and only matters when a UniFi LTE failover device is in use; otherwise it is cosmetic. Set `false` to take the network off the LTE backup link. This is read back from the controller rather than defaulted: the controller sets it automatically, and a static `true` default stamped a network the controller held as `false` back to `true` on any plan that omitted the attribute.",
      "why_not_breaking": "A description is not part of any configuration's contract: no config becomes invalid and no state migrates. The text is corrected because the released wording asserts a default the attribute no longer has, so keeping it would ship a statement known to be false about the behaviour shipping alongside it.",
      "evidence": [
        {
          "claim": "The released schema documents a default that the candidate no longer declares.",
          "kind": "measured",
          "source": "provider-contracts/schema/terraform-1.15.8.json: lte_lan description says 'Defaults to `true`'; the candidate schema has no Default for lte_lan."
        },
        {
          "claim": "Terraform never rejects a configuration on the basis of an attribute description.",
          "kind": "inferred",
          "source": "Terraform plugin protocol semantics. Reasoning, not observation."
        }
      ]
    },
    {
      "surface": "unifi_wlan",
      "attribute": "ap_group_ids",
      "field": "computed",
      "old": "false",
      "new": "true",
      "why_not_breaking": "The attribute stays Optional and gains Computed, so a configuration that sets ap_group_ids is unaffected: the configured value still wins at plan and at apply. A configuration that omits it has no working behaviour to preserve, because on the released provider omitting it aborts the apply. The controller always returns an AP group, the read path copies that value into state, and Terraform rejects the result as inconsistent with a null plan. The one semantic difference Optional+Computed introduces -- removing a previously set attribute stops planning a change to null and instead retains the value -- is not reachable on v0.101.2 either, because after removal the config is null and the apply hits the same abort. Every path this widens is a path that currently terminates in an error.",
      "evidence": [
        {
          "claim": "On the released provider, applying a unifi_wlan whose config omits ap_group_ids aborts with 'Provider produced inconsistent result after apply: .ap_group_ids: was null, but now cty.SetVal([]cty.Value{cty.StringVal(\"6a817f9bf588b98929005a32\")})'.",
          "kind": "measured",
          "source": "acceptance repro against a 10.4.57 simulation controller, apply exit=1; recorded in the wlan import/plan/apply repro run (wlan-repro2). The resource was imported, then planned and applied with the attribute omitted."
        },
        {
          "claim": "The released schema declares ap_group_ids required=false, optional=true, computed=false, so Terraform accepts a config that omits it and the failure occurs at apply rather than at validate.",
          "kind": "measured",
          "source": "provider-contracts/schema/terraform-1.15.8.json, resource_schemas.unifi_wlan.block.attributes.ap_group_ids"
        },
        {
          "claim": "Under Optional+Computed a configured value is still authoritative, so configurations that set the attribute see no change in plan or apply.",
          "kind": "inferred",
          "source": "Terraform plugin framework semantics for Optional+Computed. Reasoning, not observation."
        }
      ]
    },
    {
      "surface": "unifi_wlan",
      "attribute": "network_id",
      "field": "computed",
      "old": "false",
      "new": "true",
      "why_not_breaking": "Same transition and same argument as ap_group_ids: still Optional, so a configuration that sets network_id is unaffected, and a configuration that omits it has no working behaviour to preserve. Stated plainly, because the ledger should not launder it: the aborting apply has been observed directly for ap_group_ids and NOT for network_id. The claim that network_id behaves the same rests on reading the resource, not on a run. The read path assigns the controller's networkconf_id into state whenever it is non-empty, and the update path only carries a planned value through when that value is non-null, so an omitted network_id reaches the consistency check as null against a populated read -- the same shape that aborted for ap_group_ids. A controller wlan document always carries a non-empty networkconf_id. If that reasoning is wrong, the failure mode is that this attribute did not need Computed, which shows up as a widened schema rather than as a broken configuration.",
      "evidence": [
        {
          "claim": "The released schema declares network_id required=false, optional=true, computed=false.",
          "kind": "measured",
          "source": "provider-contracts/schema/terraform-1.15.8.json, resource_schemas.unifi_wlan.block.attributes.network_id"
        },
        {
          "claim": "The read path sets network_id from the controller response whenever it is non-empty, and the update path preserves the planned value only when it is neither null nor unknown -- so an omitted network_id is compared as null against a populated read.",
          "kind": "inferred",
          "source": "unifi/wlan_resource.go:1059-1062 (read) and unifi/wlan_resource.go:631-632 (update). Read from the source, not observed at runtime."
        },
        {
          "claim": "No recorded acceptance run names network_id as the subject of a 'provider produced inconsistent result after apply' error; only ap_group_ids, minimum_data_rate_5g_kbps, purpose, third_party_gateway, dhcp_server.dns_servers and dns.servers appear.",
          "kind": "measured",
          "source": "search of every acceptance log in the run directory for 'unexpected new value'. This is stated as the limit of the evidence, not as support for the change."
        }
      ]
    },
    {
      "surface": "unifi_network",
      "attribute": "ipv6_static_subnet",
      "field": "computed",
      "old": "false",
      "new": "true",
      "why_not_breaking": "The attribute stays Optional and gains Computed, so a configuration that sets ipv6_static_subnet is unaffected: the configured value still wins at plan and at apply. A configuration that omits it has no working behaviour to preserve, because on the released provider omitting it aborts the apply -- the controller assigns the value, the read copies it into state, and Terraform rejects the result as inconsistent with a null plan. Removing a previously set value is not a surviving path either: after removal the config is null and the apply hits the same abort. Every path this widens currently terminates in an error.",
      "evidence": [
        {
          "claim": "Applying a unifi_network whose config omits ipv6_static_subnet aborts with 'Provider produced inconsistent result after apply: .ipv6_static_subnet: was null, but now cty.StringVal(\"fd41:9::1/64\")'.",
          "kind": "measured",
          "source": "acceptance reproduction against a 10.4.57 simulation controller (ghcr.io/jamesbraid/unifi-network@sha256:584be3a2...), provider built at 5f00f7c4, apply exit=1. Recorded in the dhcp_v6_server reproduction run."
        },
        {
          "claim": "The released schema declares ipv6_static_subnet required=false, optional=true, computed=false.",
          "kind": "measured",
          "source": "provider-contracts/schema/terraform-1.15.8.json, unifi_network.ipv6_static_subnet"
        },
        {
          "claim": "Under Optional+Computed a configured value remains authoritative, so configurations that set the attribute see no change.",
          "kind": "inferred",
          "source": "Terraform plugin framework semantics for Optional+Computed. Reasoning, not observation."
        }
      ]
    },
    {
      "surface": "unifi_network",
      "attribute": "dhcp_v6_server.start",
      "field": "computed",
      "old": "false",
      "new": "true",
      "why_not_breaking": "The attribute stays Optional and gains Computed, so a configuration that sets dhcp_v6_server.start is unaffected: the configured value still wins at plan and at apply. A configuration that omits it has no working behaviour to preserve, because on the released provider omitting it aborts the apply -- the controller assigns the value, the read copies it into state, and Terraform rejects the result as inconsistent with a null plan. Removing a previously set value is not a surviving path either: after removal the config is null and the apply hits the same abort. Every path this widens currently terminates in an error.",
      "evidence": [
        {
          "claim": "Applying a unifi_network whose config omits dhcp_v6_server.start aborts with 'Provider produced inconsistent result after apply: .dhcp_v6_server.start: was null, but now cty.StringVal(\"fd41:9::2\")'.",
          "kind": "measured",
          "source": "acceptance reproduction against a 10.4.57 simulation controller (ghcr.io/jamesbraid/unifi-network@sha256:584be3a2...), provider built at 5f00f7c4, apply exit=1. Recorded in the dhcp_v6_server reproduction run."
        },
        {
          "claim": "The released schema declares dhcp_v6_server.start required=false, optional=true, computed=false.",
          "kind": "measured",
          "source": "provider-contracts/schema/terraform-1.15.8.json, unifi_network.dhcp_v6_server.start"
        },
        {
          "claim": "Under Optional+Computed a configured value remains authoritative, so configurations that set the attribute see no change.",
          "kind": "inferred",
          "source": "Terraform plugin framework semantics for Optional+Computed. Reasoning, not observation."
        }
      ]
    },
    {
      "surface": "unifi_network",
      "attribute": "dhcp_v6_server.stop",
      "field": "computed",
      "old": "false",
      "new": "true",
      "why_not_breaking": "The attribute stays Optional and gains Computed, so a configuration that sets dhcp_v6_server.stop is unaffected: the configured value still wins at plan and at apply. A configuration that omits it has no working behaviour to preserve, because on the released provider omitting it aborts the apply -- the controller assigns the value, the read copies it into state, and Terraform rejects the result as inconsistent with a null plan. Removing a previously set value is not a surviving path either: after removal the config is null and the apply hits the same abort. Every path this widens currently terminates in an error.",
      "evidence": [
        {
          "claim": "Applying a unifi_network whose config omits dhcp_v6_server.stop aborts with 'Provider produced inconsistent result after apply: .dhcp_v6_server.stop: was null, but now cty.StringVal(\"fd41:9::7d1\")'.",
          "kind": "measured",
          "source": "acceptance reproduction against a 10.4.57 simulation controller (ghcr.io/jamesbraid/unifi-network@sha256:584be3a2...), provider built at 5f00f7c4, apply exit=1. Recorded in the dhcp_v6_server reproduction run."
        },
        {
          "claim": "The released schema declares dhcp_v6_server.stop required=false, optional=true, computed=false.",
          "kind": "measured",
          "source": "provider-contracts/schema/terraform-1.15.8.json, unifi_network.dhcp_v6_server.stop"
        },
        {
          "claim": "Under Optional+Computed a configured value remains authoritative, so configurations that set the attribute see no change.",
          "kind": "inferred",
          "source": "Terraform plugin framework semantics for Optional+Computed. Reasoning, not observation."
        }
      ]
    },
    {
      "surface": "unifi_network",
      "attribute": "ip_aliases",
      "field": "computed",
      "old": "false",
      "new": "true",
      "why_not_breaking": "The attribute stays Optional and gains Computed, so a configuration that sets ip_aliases is unaffected: the configured value still wins at plan and at apply. A configuration that omits it has no working behaviour to preserve. On the released provider the read path sets ip_aliases to null unconditionally, and ip_aliases is in the wire mask, so the write sends the zero the mapper pre-seeds and the controller -- which treats the field as authoritative -- discards whatever it held. Omitting the attribute today destroys the value on every apply, including applies that change something unrelated. The one semantic difference Optional+Computed introduces, that removing a previously set attribute retains the value rather than planning a change to null, replaces a behaviour that was already destructive and never expressible: on v0.101.2 there is no way to say \"leave this alone\", because null and \"clear it\" are the same request.",
      "evidence": [
        {
          "claim": "On the released provider, an apply that changes only an unrelated attribute sends ip_aliases as its zero and the controller clears the value it held.",
          "kind": "measured",
          "source": "controller run against 10.4.57: the encoded request body was captured and each link checked separately -- the field is present in the masked update, it encodes as its zero when unset, and the controller clears a field on receiving one."
        },
        {
          "claim": "The released schema declares ip_aliases optional=true, computed=false.",
          "kind": "measured",
          "source": "provider-contracts/schema/terraform-1.15.8.json, resource_schemas.unifi_network.block.attributes.ip_aliases"
        },
        {
          "claim": "Under Optional+Computed a configured value remains authoritative, so configurations that set the attribute see no change in plan or apply.",
          "kind": "inferred",
          "source": "Terraform plugin framework semantics for Optional+Computed. Reasoning, not observation."
        }
      ]
    },
    {
      "surface": "unifi_network",
      "attribute": "nat_outbound_ip_addresses",
      "field": "computed",
      "old": "false",
      "new": "true",
      "why_not_breaking": "The attribute stays Optional and gains Computed, so a configuration that sets nat_outbound_ip_addresses is unaffected: the configured value still wins at plan and at apply. A configuration that omits it has no working behaviour to preserve. On the released provider the read path sets nat_outbound_ip_addresses to null unconditionally, and nat_outbound_ip_addresses is in the wire mask, so the write sends the zero the mapper pre-seeds and the controller -- which treats the field as authoritative -- discards whatever it held. Omitting the attribute today destroys the value on every apply, including applies that change something unrelated. The one semantic difference Optional+Computed introduces, that removing a previously set attribute retains the value rather than planning a change to null, replaces a behaviour that was already destructive and never expressible: on v0.101.2 there is no way to say \"leave this alone\", because null and \"clear it\" are the same request.",
      "evidence": [
        {
          "claim": "On the released provider, an apply that changes only an unrelated attribute sends nat_outbound_ip_addresses as its zero and the controller clears the value it held.",
          "kind": "measured",
          "source": "controller run against 10.4.57: the encoded request body was captured and each link checked separately -- the field is present in the masked update, it encodes as its zero when unset, and the controller clears a field on receiving one."
        },
        {
          "claim": "The released schema declares nat_outbound_ip_addresses optional=true, computed=false.",
          "kind": "measured",
          "source": "provider-contracts/schema/terraform-1.15.8.json, resource_schemas.unifi_network.block.attributes.nat_outbound_ip_addresses"
        },
        {
          "claim": "Under Optional+Computed a configured value remains authoritative, so configurations that set the attribute see no change in plan or apply.",
          "kind": "inferred",
          "source": "Terraform plugin framework semantics for Optional+Computed. Reasoning, not observation."
        }
      ]
    },
    {
      "surface": "unifi_network",
      "attribute": "ipv6_pd_prefixid",
      "field": "computed",
      "old": "false",
      "new": "true",
      "why_not_breaking": "The attribute stays Optional and gains Computed, so a configuration that sets ipv6_pd_prefixid is unaffected: the configured value still wins at plan and at apply. A configuration that omits it has no working behaviour to preserve. On the released provider the read path sets ipv6_pd_prefixid to null unconditionally, and ipv6_pd_prefixid is in the wire mask, so the write sends the zero the mapper pre-seeds and the controller -- which treats the field as authoritative -- discards whatever it held. Omitting the attribute today destroys the value on every apply, including applies that change something unrelated. The one semantic difference Optional+Computed introduces, that removing a previously set attribute retains the value rather than planning a change to null, replaces a behaviour that was already destructive and never expressible: on v0.101.2 there is no way to say \"leave this alone\", because null and \"clear it\" are the same request.",
      "evidence": [
        {
          "claim": "On the released provider, an apply that changes only an unrelated attribute sends ipv6_pd_prefixid as its zero and the controller clears the value it held.",
          "kind": "measured",
          "source": "controller run against 10.4.57: the encoded request body was captured and each link checked separately -- the field is present in the masked update, it encodes as its zero when unset, and the controller clears a field on receiving one."
        },
        {
          "claim": "The released schema declares ipv6_pd_prefixid optional=true, computed=false.",
          "kind": "measured",
          "source": "provider-contracts/schema/terraform-1.15.8.json, resource_schemas.unifi_network.block.attributes.ipv6_pd_prefixid"
        },
        {
          "claim": "Under Optional+Computed a configured value remains authoritative, so configurations that set the attribute see no change in plan or apply.",
          "kind": "inferred",
          "source": "Terraform plugin framework semantics for Optional+Computed. Reasoning, not observation."
        }
      ]
    },
    {
      "surface": "unifi_firewall_policy",
      "attribute": "source.match_mac",
      "field": "presence",
      "old": "absent",
      "new": "present",
      "why_not_breaking": "An added Optional+Computed attribute cannot invalidate an existing configuration, because a configuration that does not mention it is exactly what worked before: it plans unknown, UseStateForUnknown resolves it from the value just read, and nothing the practitioner wrote changes meaning. It is not Required, so no existing configuration becomes incomplete. WHAT IT REPLACES IS THE BREAKING BEHAVIOUR. match_mac is emitted by go-unifi without omitempty and the provider did not model it, so endpointModelToSource built a fresh struct with the Go zero and the masked update sent the whole nested object -- a field mask names top-level keys, so masking `source` sends everything inside it. match_opposite_ips true against a specific IP list means the policy matches every address EXCEPT that list, so resetting it to false turned \"block everything but these\" into \"block only these\" while the policy stayed visibly present and enforcing. TRANSITIONAL. The baseline tracks the last release and rebases when one ships. This entry licenses the attribute as a delta against v0.101.2; once the baseline moves to the release containing it the delta is zero and this entry is redundant and should be dropped.",
      "evidence": [
        {
          "claim": "The controller holds match_mac true on a policy whose matching_target is IP with a specific address list, and an apply that changed only the description reset it, inverting the rule.",
          "kind": "measured",
          "source": "controller run against 10.4.57; layers 1-3 measured separately and the composition confirmed end to end through the provider."
        },
        {
          "claim": "unifi.FirewallPolicySource and FirewallPolicyDestination declare match_mac with no omitempty, so it is emitted whatever its value.",
          "kind": "measured",
          "source": "go-unifi v1.103.0 firewall_policy.generated.go, struct tag."
        },
        {
          "claim": "Under matching_target ANY the controller reports these flags false, because an inversion flag with nothing to invert has no meaning. The attribute is therefore meaningful only under specific matching targets; a ConfigValidator rejecting the combination is a separate decision, noted and not encoded.",
          "kind": "measured",
          "source": "controller run against 10.4.57, first measurement of the same field."
        }
      ]
    },
    {
      "surface": "unifi_firewall_policy",
      "attribute": "source.match_opposite_networks",
      "field": "presence",
      "old": "absent",
      "new": "present",
      "why_not_breaking": "An added Optional+Computed attribute cannot invalidate an existing configuration, because a configuration that does not mention it is exactly what worked before: it plans unknown, UseStateForUnknown resolves it from the value just read, and nothing the practitioner wrote changes meaning. It is not Required, so no existing configuration becomes incomplete. WHAT IT REPLACES IS THE BREAKING BEHAVIOUR. match_opposite_networks is emitted by go-unifi without omitempty and the provider did not model it, so endpointModelToSource built a fresh struct with the Go zero and the masked update sent the whole nested object -- a field mask names top-level keys, so masking `source` sends everything inside it. match_opposite_ips true against a specific IP list means the policy matches every address EXCEPT that list, so resetting it to false turned \"block everything but these\" into \"block only these\" while the policy stayed visibly present and enforcing. TRANSITIONAL. The baseline tracks the last release and rebases when one ships. This entry licenses the attribute as a delta against v0.101.2; once the baseline moves to the release containing it the delta is zero and this entry is redundant and should be dropped.",
      "evidence": [
        {
          "claim": "The controller holds match_opposite_networks true on a policy whose matching_target is IP with a specific address list, and an apply that changed only the description reset it, inverting the rule.",
          "kind": "measured",
          "source": "controller run against 10.4.57; layers 1-3 measured separately and the composition confirmed end to end through the provider."
        },
        {
          "claim": "unifi.FirewallPolicySource and FirewallPolicyDestination declare match_opposite_networks with no omitempty, so it is emitted whatever its value.",
          "kind": "measured",
          "source": "go-unifi v1.103.0 firewall_policy.generated.go, struct tag."
        },
        {
          "claim": "Under matching_target ANY the controller reports these flags false, because an inversion flag with nothing to invert has no meaning. The attribute is therefore meaningful only under specific matching targets; a ConfigValidator rejecting the combination is a separate decision, noted and not encoded.",
          "kind": "measured",
          "source": "controller run against 10.4.57, first measurement of the same field."
        }
      ]
    },
    {
      "surface": "unifi_firewall_policy",
      "attribute": "source.match_opposite_ports",
      "field": "presence",
      "old": "absent",
      "new": "present",
      "why_not_breaking": "An added Optional+Computed attribute cannot invalidate an existing configuration, because a configuration that does not mention it is exactly what worked before: it plans unknown, UseStateForUnknown resolves it from the value just read, and nothing the practitioner wrote changes meaning. It is not Required, so no existing configuration becomes incomplete. WHAT IT REPLACES IS THE BREAKING BEHAVIOUR. match_opposite_ports is emitted by go-unifi without omitempty and the provider did not model it, so endpointModelToSource built a fresh struct with the Go zero and the masked update sent the whole nested object -- a field mask names top-level keys, so masking `source` sends everything inside it. match_opposite_ips true against a specific IP list means the policy matches every address EXCEPT that list, so resetting it to false turned \"block everything but these\" into \"block only these\" while the policy stayed visibly present and enforcing. TRANSITIONAL. The baseline tracks the last release and rebases when one ships. This entry licenses the attribute as a delta against v0.101.2; once the baseline moves to the release containing it the delta is zero and this entry is redundant and should be dropped.",
      "evidence": [
        {
          "claim": "The controller holds match_opposite_ports true on a policy whose matching_target is IP with a specific address list, and an apply that changed only the description reset it, inverting the rule.",
          "kind": "measured",
          "source": "controller run against 10.4.57; layers 1-3 measured separately and the composition confirmed end to end through the provider."
        },
        {
          "claim": "unifi.FirewallPolicySource and FirewallPolicyDestination declare match_opposite_ports with no omitempty, so it is emitted whatever its value.",
          "kind": "measured",
          "source": "go-unifi v1.103.0 firewall_policy.generated.go, struct tag."
        },
        {
          "claim": "Under matching_target ANY the controller reports these flags false, because an inversion flag with nothing to invert has no meaning. The attribute is therefore meaningful only under specific matching targets; a ConfigValidator rejecting the combination is a separate decision, noted and not encoded.",
          "kind": "measured",
          "source": "controller run against 10.4.57, first measurement of the same field."
        }
      ]
    },
    {
      "surface": "unifi_firewall_policy",
      "attribute": "destination.match_mac",
      "field": "presence",
      "old": "absent",
      "new": "present",
      "why_not_breaking": "An added Optional+Computed attribute cannot invalidate an existing configuration, because a configuration that does not mention it is exactly what worked before: it plans unknown, UseStateForUnknown resolves it from the value just read, and nothing the practitioner wrote changes meaning. It is not Required, so no existing configuration becomes incomplete. WHAT IT REPLACES IS THE BREAKING BEHAVIOUR. match_mac is emitted by go-unifi without omitempty and the provider did not model it, so endpointModelToDestination built a fresh struct with the Go zero and the masked update sent the whole nested object -- a field mask names top-level keys, so masking `destination` sends everything inside it. match_opposite_ips true against a specific IP list means the policy matches every address EXCEPT that list, so resetting it to false turned \"block everything but these\" into \"block only these\" while the policy stayed visibly present and enforcing. TRANSITIONAL. The baseline tracks the last release and rebases when one ships. This entry licenses the attribute as a delta against v0.101.2; once the baseline moves to the release containing it the delta is zero and this entry is redundant and should be dropped.",
      "evidence": [
        {
          "claim": "The controller holds match_mac true on a policy whose matching_target is IP with a specific address list, and an apply that changed only the description reset it, inverting the rule.",
          "kind": "measured",
          "source": "controller run against 10.4.57; layers 1-3 measured separately and the composition confirmed end to end through the provider."
        },
        {
          "claim": "unifi.FirewallPolicySource and FirewallPolicyDestination declare match_mac with no omitempty, so it is emitted whatever its value.",
          "kind": "measured",
          "source": "go-unifi v1.103.0 firewall_policy.generated.go, struct tag."
        },
        {
          "claim": "Under matching_target ANY the controller reports these flags false, because an inversion flag with nothing to invert has no meaning. The attribute is therefore meaningful only under specific matching targets; a ConfigValidator rejecting the combination is a separate decision, noted and not encoded.",
          "kind": "measured",
          "source": "controller run against 10.4.57, first measurement of the same field."
        }
      ]
    },
    {
      "surface": "unifi_firewall_policy",
      "attribute": "destination.match_opposite_ips",
      "field": "presence",
      "old": "absent",
      "new": "present",
      "why_not_breaking": "An added Optional+Computed attribute cannot invalidate an existing configuration, because a configuration that does not mention it is exactly what worked before: it plans unknown, UseStateForUnknown resolves it from the value just read, and nothing the practitioner wrote changes meaning. It is not Required, so no existing configuration becomes incomplete. WHAT IT REPLACES IS THE BREAKING BEHAVIOUR. match_opposite_ips is emitted by go-unifi without omitempty and the provider did not model it, so endpointModelToDestination built a fresh struct with the Go zero and the masked update sent the whole nested object -- a field mask names top-level keys, so masking `destination` sends everything inside it. match_opposite_ips true against a specific IP list means the policy matches every address EXCEPT that list, so resetting it to false turned \"block everything but these\" into \"block only these\" while the policy stayed visibly present and enforcing. TRANSITIONAL. The baseline tracks the last release and rebases when one ships. This entry licenses the attribute as a delta against v0.101.2; once the baseline moves to the release containing it the delta is zero and this entry is redundant and should be dropped.",
      "evidence": [
        {
          "claim": "The controller holds match_opposite_ips true on a policy whose matching_target is IP with a specific address list, and an apply that changed only the description reset it, inverting the rule.",
          "kind": "measured",
          "source": "controller run against 10.4.57; layers 1-3 measured separately and the composition confirmed end to end through the provider."
        },
        {
          "claim": "unifi.FirewallPolicySource and FirewallPolicyDestination declare match_opposite_ips with no omitempty, so it is emitted whatever its value.",
          "kind": "measured",
          "source": "go-unifi v1.103.0 firewall_policy.generated.go, struct tag."
        },
        {
          "claim": "Under matching_target ANY the controller reports these flags false, because an inversion flag with nothing to invert has no meaning. The attribute is therefore meaningful only under specific matching targets; a ConfigValidator rejecting the combination is a separate decision, noted and not encoded.",
          "kind": "measured",
          "source": "controller run against 10.4.57, first measurement of the same field."
        }
      ]
    },
    {
      "surface": "unifi_firewall_policy",
      "attribute": "destination.match_opposite_networks",
      "field": "presence",
      "old": "absent",
      "new": "present",
      "why_not_breaking": "An added Optional+Computed attribute cannot invalidate an existing configuration, because a configuration that does not mention it is exactly what worked before: it plans unknown, UseStateForUnknown resolves it from the value just read, and nothing the practitioner wrote changes meaning. It is not Required, so no existing configuration becomes incomplete. WHAT IT REPLACES IS THE BREAKING BEHAVIOUR. match_opposite_networks is emitted by go-unifi without omitempty and the provider did not model it, so endpointModelToDestination built a fresh struct with the Go zero and the masked update sent the whole nested object -- a field mask names top-level keys, so masking `destination` sends everything inside it. match_opposite_ips true against a specific IP list means the policy matches every address EXCEPT that list, so resetting it to false turned \"block everything but these\" into \"block only these\" while the policy stayed visibly present and enforcing. TRANSITIONAL. The baseline tracks the last release and rebases when one ships. This entry licenses the attribute as a delta against v0.101.2; once the baseline moves to the release containing it the delta is zero and this entry is redundant and should be dropped.",
      "evidence": [
        {
          "claim": "The controller holds match_opposite_networks true on a policy whose matching_target is IP with a specific address list, and an apply that changed only the description reset it, inverting the rule.",
          "kind": "measured",
          "source": "controller run against 10.4.57; layers 1-3 measured separately and the composition confirmed end to end through the provider."
        },
        {
          "claim": "unifi.FirewallPolicySource and FirewallPolicyDestination declare match_opposite_networks with no omitempty, so it is emitted whatever its value.",
          "kind": "measured",
          "source": "go-unifi v1.103.0 firewall_policy.generated.go, struct tag."
        },
        {
          "claim": "Under matching_target ANY the controller reports these flags false, because an inversion flag with nothing to invert has no meaning. The attribute is therefore meaningful only under specific matching targets; a ConfigValidator rejecting the combination is a separate decision, noted and not encoded.",
          "kind": "measured",
          "source": "controller run against 10.4.57, first measurement of the same field."
        }
      ]
    },
    {
      "surface": "unifi_firewall_policy",
      "attribute": "destination.match_opposite_ports",
      "field": "presence",
      "old": "absent",
      "new": "present",
      "why_not_breaking": "An added Optional+Computed attribute cannot invalidate an existing configuration, because a configuration that does not mention it is exactly what worked before: it plans unknown, UseStateForUnknown resolves it from the value just read, and nothing the practitioner wrote changes meaning. It is not Required, so no existing configuration becomes incomplete. WHAT IT REPLACES IS THE BREAKING BEHAVIOUR. match_opposite_ports is emitted by go-unifi without omitempty and the provider did not model it, so endpointModelToDestination built a fresh struct with the Go zero and the masked update sent the whole nested object -- a field mask names top-level keys, so masking `destination` sends everything inside it. match_opposite_ips true against a specific IP list means the policy matches every address EXCEPT that list, so resetting it to false turned \"block everything but these\" into \"block only these\" while the policy stayed visibly present and enforcing. TRANSITIONAL. The baseline tracks the last release and rebases when one ships. This entry licenses the attribute as a delta against v0.101.2; once the baseline moves to the release containing it the delta is zero and this entry is redundant and should be dropped.",
      "evidence": [
        {
          "claim": "The controller holds match_opposite_ports true on a policy whose matching_target is IP with a specific address list, and an apply that changed only the description reset it, inverting the rule.",
          "kind": "measured",
          "source": "controller run against 10.4.57; layers 1-3 measured separately and the composition confirmed end to end through the provider."
        },
        {
          "claim": "unifi.FirewallPolicySource and FirewallPolicyDestination declare match_opposite_ports with no omitempty, so it is emitted whatever its value.",
          "kind": "measured",
          "source": "go-unifi v1.103.0 firewall_policy.generated.go, struct tag."
        },
        {
          "claim": "Under matching_target ANY the controller reports these flags false, because an inversion flag with nothing to invert has no meaning. The attribute is therefore meaningful only under specific matching targets; a ConfigValidator rejecting the combination is a separate decision, noted and not encoded.",
          "kind": "measured",
          "source": "controller run against 10.4.57, first measurement of the same field."
        }
      ]
    },
    {
      "surface": "unifi_firewall_policy",
      "attribute": "source.match_opposite_ips",
      "field": "presence",
      "old": "absent",
      "new": "present",
      "why_not_breaking": "An added Optional+Computed attribute cannot invalidate an existing configuration, because a configuration that does not mention it is exactly what worked before: it plans unknown, UseStateForUnknown resolves it from the value just read, and nothing the practitioner wrote changes meaning. It is not Required, so no existing configuration becomes incomplete. WHAT IT REPLACES IS THE BREAKING BEHAVIOUR. match_opposite_ips is emitted by go-unifi without omitempty and the provider did not model it, so endpointModelToSource built a fresh struct with the Go zero and the masked update sent the whole nested object -- a field mask names top-level keys, so masking `source` sends everything inside it. match_opposite_ips true against a specific IP list means the policy matches every address EXCEPT that list, so resetting it to false turned \"block everything but these\" into \"block only these\" while the policy stayed visibly present and enforcing. TRANSITIONAL. The baseline tracks the last release and rebases when one ships. This entry licenses the attribute as a delta against v0.101.2; once the baseline moves to the release containing it the delta is zero and this entry is redundant and should be dropped.",
      "evidence": [
        {
          "claim": "The controller holds match_opposite_ips true on a policy whose matching_target is IP with a specific address list, and an apply that changed only the description reset it, inverting the rule.",
          "kind": "measured",
          "source": "controller run against 10.4.57; layers 1-3 measured separately and the composition confirmed end to end through the provider."
        },
        {
          "claim": "unifi.FirewallPolicySource and FirewallPolicyDestination declare match_opposite_ips with no omitempty, so it is emitted whatever its value.",
          "kind": "measured",
          "source": "go-unifi v1.103.0 firewall_policy.generated.go, struct tag."
        },
        {
          "claim": "Under matching_target ANY the controller reports these flags false, because an inversion flag with nothing to invert has no meaning. The attribute is therefore meaningful only under specific matching targets; a ConfigValidator rejecting the combination is a separate decision, noted and not encoded.",
          "kind": "measured",
          "source": "controller run against 10.4.57, first measurement of the same field."
        }
      ]
    }
  ]
}
